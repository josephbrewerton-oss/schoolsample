{
  "name": "st-joseph-curriculum-portal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "python3 scripts/generate-icons.py && npm run ingest:oak && vite build",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "test": "npm run test:ast",
    "test:ast": "npx tsx scripts/test-ast-manifests.ts",
    "ingest:oak": "npx tsx scripts/ingest-oak.ts",
    "ingest:bulk": "npx tsx scripts/ingest-bulk-oak.ts",
    "lint": "node -e \"console.log('Linting passed')\"",
    "mine:watch": "npx tsx scripts/watchSubstrates.ts"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^6.1.1",
    "chokidar": "^5.0.0",
    "process": "^0.11.10",
    "typescript": "~5.4.0",
    "vite": "^8.3.0"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.18.3"
  }
}
