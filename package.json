{
  "name": "st-joseph-curriculum-portal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 3000",
    "build": "python3 scripts/generate-icons.py && npm run ingest:oak && vite build && node -e \"try{fs.rmSync('dist/internal-key.json',{force:true})}catch(e){}\"",
    "preview": "vite preview --host 0.0.0.0 --port 3000",
    "test": "npm run test:ast",
    "test:ast": "npx tsx scripts/test-ast-manifests.ts",
    "ingest:oak": "npx tsx scripts/ingest-oak.ts",
    "ingest:bulk": "npx tsx scripts/ingest-bulk-oak.ts",
    "export:dist": "npx tsx scripts/export-distribution.ts",
    "lint": "tsc --noEmit",
    "mine:watch": "npx tsx scripts/watchSubstrates.ts"
  },
  "overrides": {
    "esbuild": "^0.28.1"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^6.1.1",
    "chokidar": "^5.0.0",
    "process": "^0.11.10",
    "typescript": "^5.6.3",
    "vite": "^8.3.0"
  },
  "browserslist": {
    "production": [
      ">0.5%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 3 chrome version",
      "last 3 firefox version",
      "last 5 safari version"
    ]
  },
  "engines": {
    "node": ">=20.0"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.85",
    "idb": "^8.0.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "react-router-dom": "^7.18.3"
  }
}
